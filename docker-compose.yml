services:
  app:
    image: ${DOCKER_IMAGE:-nikulina2454/devops-for-developers-project-74}
    build:
      context: .
      dockerfile: Dockerfile.production
    working_dir: /app
    command: make test
